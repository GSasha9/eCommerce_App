import type { IRoutes } from '../shared/models/interfaces';

export const ROUTES: IRoutes = {
  HOME: '/',
  HOME_ALT: '/home',
  LOGIN: '/login',
  REG: '/registration',
  PRODUCTS: '/products',
  BASKET: '/basket',
  NOT_FOUND: '/404',
};
